# Copy to .env for local MiniOxygen (never commit real secrets).
# With no store linked, Hydrogen uses mock.shop demo catalog data.
SESSION_SECRET="replace-me-with-a-long-random-string"

# After linking a real store (npx shopify hydrogen link && npx shopify hydrogen env pull):
# PUBLIC_STORE_DOMAIN="your-store.myshopify.com"
# PUBLIC_STOREFRONT_API_TOKEN="..."
# PUBLIC_STOREFRONT_ID="..."
# PUBLIC_CHECKOUT_DOMAIN="..."
